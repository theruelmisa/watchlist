import React from 'react';
import { SearchBar, MovieList } from '../../components';

const HomePage = () => {
    return (
        <div>
            <SearchBar />
            <MovieList />
        </div>
    )
}

export default HomePage
