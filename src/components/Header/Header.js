import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>Watch List Logo</h1>
            <button>Sign in</button>
        </header>
    )
};

export default Header;
